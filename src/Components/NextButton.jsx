import React from 'react'

const NextButton = () => {
  return (
     <div id = "buttonRow" className="row align-items-center justify-content-end">
       {/* <h4 onclick="nextForm()">Nästa</h4> onClick="nextForm()"  */}
      <button type="button" className="buttonNext" >Nästa</button>
    </div> 
  )
}

export default NextButton